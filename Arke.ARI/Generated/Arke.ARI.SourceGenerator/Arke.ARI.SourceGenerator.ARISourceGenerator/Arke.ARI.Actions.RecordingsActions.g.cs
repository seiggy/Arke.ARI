// <auto-generated/>
using System;
using System.Collections.Generic;
using System.Threading.Tasks;
using Arke.ARI.Models;
#nullable enable

namespace Arke.ARI.Actions
{
    /// <summary>
    /// Implementation of Recordings operations
    /// </summary>
    public sealed class RecordingsActions : IRecordingsActions
    {
        private readonly Arke.ARI.AriClient _client;

        public RecordingsActions(Arke.ARI.AriClient client)
        {
            _client = client ?? throw new ArgumentNullException(nameof(client));
        }

        public List<StoredRecording> ListStored()
        {
            return ListStoredAsync().GetAwaiter().GetResult();
        }

        public async Task<List<StoredRecording>> ListStoredAsync()
        {
            var url = "recordings/stored";
            return await _client.GetAsync<List<StoredRecording>>(url);
        }

        public StoredRecording GetStored(string recordingName)
        {
            return GetStoredAsync(recordingName: recordingName).GetAwaiter().GetResult();
        }

        public async Task<StoredRecording> GetStoredAsync(string recordingName)
        {
            var url = "recordings/stored/{recordingName}";
            return await _client.GetAsync<StoredRecording>(url);
        }

        public void DeleteStored(string recordingName)
        {
            DeleteStoredAsync(recordingName: recordingName).GetAwaiter().GetResult();
        }

        public async Task DeleteStoredAsync(string recordingName)
        {
            var url = "recordings/stored/{recordingName}";
            await _client.DeleteAsync(url);
        }

        public byte[] GetStoredFile(string recordingName)
        {
            return GetStoredFileAsync(recordingName: recordingName).GetAwaiter().GetResult();
        }

        public async Task<byte[]> GetStoredFileAsync(string recordingName)
        {
            var url = "recordings/stored/{recordingName}/file";
            return await _client.GetAsync<byte[]>(url);
        }

        public StoredRecording CopyStored(string recordingName, string destinationRecordingName)
        {
            return CopyStoredAsync(recordingName: recordingName, destinationRecordingName: destinationRecordingName).GetAwaiter().GetResult();
        }

        public async Task<StoredRecording> CopyStoredAsync(string recordingName, string destinationRecordingName)
        {
            var url = "recordings/stored/{recordingName}/copy";
            if (!string.IsNullOrEmpty(destinationRecordingName)) url += (url.Contains("?") ? "&" : "?") + "destinationRecordingName=" + System.Uri.EscapeDataString(destinationRecordingName);
            return await _client.PostAsync<StoredRecording>(url);
        }

        public LiveRecording GetLive(string recordingName)
        {
            return GetLiveAsync(recordingName: recordingName).GetAwaiter().GetResult();
        }

        public async Task<LiveRecording> GetLiveAsync(string recordingName)
        {
            var url = "recordings/live/{recordingName}";
            return await _client.GetAsync<LiveRecording>(url);
        }

        public void Cancel(string recordingName)
        {
            CancelAsync(recordingName: recordingName).GetAwaiter().GetResult();
        }

        public async Task CancelAsync(string recordingName)
        {
            var url = "recordings/live/{recordingName}";
            await _client.DeleteAsync(url);
        }

        public void Stop(string recordingName)
        {
            StopAsync(recordingName: recordingName).GetAwaiter().GetResult();
        }

        public async Task StopAsync(string recordingName)
        {
            var url = "recordings/live/{recordingName}/stop";
            await _client.PostAsync(url);
        }

        public void Pause(string recordingName)
        {
            PauseAsync(recordingName: recordingName).GetAwaiter().GetResult();
        }

        public async Task PauseAsync(string recordingName)
        {
            var url = "recordings/live/{recordingName}/pause";
            await _client.PostAsync(url);
        }

        public void Unpause(string recordingName)
        {
            UnpauseAsync(recordingName: recordingName).GetAwaiter().GetResult();
        }

        public async Task UnpauseAsync(string recordingName)
        {
            var url = "recordings/live/{recordingName}/pause";
            await _client.DeleteAsync(url);
        }

        public void Mute(string recordingName)
        {
            MuteAsync(recordingName: recordingName).GetAwaiter().GetResult();
        }

        public async Task MuteAsync(string recordingName)
        {
            var url = "recordings/live/{recordingName}/mute";
            await _client.PostAsync(url);
        }

        public void Unmute(string recordingName)
        {
            UnmuteAsync(recordingName: recordingName).GetAwaiter().GetResult();
        }

        public async Task UnmuteAsync(string recordingName)
        {
            var url = "recordings/live/{recordingName}/mute";
            await _client.DeleteAsync(url);
        }

    }
}
