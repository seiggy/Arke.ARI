// <auto-generated/>
using System;
using System.Collections.Generic;
using System.Threading.Tasks;
using Arke.ARI.Models;
#nullable enable

namespace Arke.ARI.Actions
{
    /// <summary>
    /// Implementation of DeviceStates operations
    /// </summary>
    public sealed class DeviceStatesActions : IDeviceStatesActions
    {
        private readonly Arke.ARI.AriClient _client;

        public DeviceStatesActions(Arke.ARI.AriClient client)
        {
            _client = client ?? throw new ArgumentNullException(nameof(client));
        }

        public List<DeviceState> List()
        {
            return ListAsync().GetAwaiter().GetResult();
        }

        public async Task<List<DeviceState>> ListAsync()
        {
            var url = "deviceStates";
            return await _client.GetAsync<List<DeviceState>>(url);
        }

        public DeviceState Get(string deviceName)
        {
            return GetAsync(deviceName: deviceName).GetAwaiter().GetResult();
        }

        public async Task<DeviceState> GetAsync(string deviceName)
        {
            var url = "deviceStates/{deviceName}";
            return await _client.GetAsync<DeviceState>(url);
        }

        public void Update(string deviceName, string deviceState)
        {
            UpdateAsync(deviceName: deviceName, deviceState: deviceState).GetAwaiter().GetResult();
        }

        public async Task UpdateAsync(string deviceName, string deviceState)
        {
            var url = "deviceStates/{deviceName}";
            if (!string.IsNullOrEmpty(deviceState)) url += (url.Contains("?") ? "&" : "?") + "deviceState=" + System.Uri.EscapeDataString(deviceState);
            await _client.PutAsync(url);
        }

        public void Delete(string deviceName)
        {
            DeleteAsync(deviceName: deviceName).GetAwaiter().GetResult();
        }

        public async Task DeleteAsync(string deviceName)
        {
            var url = "deviceStates/{deviceName}";
            await _client.DeleteAsync(url);
        }

    }
}
