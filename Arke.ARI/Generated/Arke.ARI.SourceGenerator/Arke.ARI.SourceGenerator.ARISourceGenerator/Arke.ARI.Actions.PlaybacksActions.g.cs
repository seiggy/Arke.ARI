// <auto-generated/>
using System;
using System.Collections.Generic;
using System.Threading.Tasks;
using Arke.ARI.Models;
#nullable enable

namespace Arke.ARI.Actions
{
    /// <summary>
    /// Implementation of Playbacks operations
    /// </summary>
    public sealed class PlaybacksActions : IPlaybacksActions
    {
        private readonly Arke.ARI.AriClient _client;

        public PlaybacksActions(Arke.ARI.AriClient client)
        {
            _client = client ?? throw new ArgumentNullException(nameof(client));
        }

        public Playback Get(string playbackId)
        {
            return GetAsync(playbackId: playbackId).GetAwaiter().GetResult();
        }

        public async Task<Playback> GetAsync(string playbackId)
        {
            var url = "playbacks/{playbackId}";
            return await _client.GetAsync<Playback>(url);
        }

        public void Stop(string playbackId)
        {
            StopAsync(playbackId: playbackId).GetAwaiter().GetResult();
        }

        public async Task StopAsync(string playbackId)
        {
            var url = "playbacks/{playbackId}";
            await _client.DeleteAsync(url);
        }

        public void Control(string playbackId, string operation)
        {
            ControlAsync(playbackId: playbackId, operation: operation).GetAwaiter().GetResult();
        }

        public async Task ControlAsync(string playbackId, string operation)
        {
            var url = "playbacks/{playbackId}/control";
            if (!string.IsNullOrEmpty(operation)) url += (url.Contains("?") ? "&" : "?") + "operation=" + System.Uri.EscapeDataString(operation);
            await _client.PostAsync(url);
        }

    }
}
