// <auto-generated/>
using System;
using System.Collections.Generic;
using System.Threading.Tasks;
using Arke.ARI.Models;
#nullable enable

namespace Arke.ARI.Actions
{
    /// <summary>
    /// Implementation of Sounds operations
    /// </summary>
    public sealed class SoundsActions : ISoundsActions
    {
        private readonly Arke.ARI.AriClient _client;

        public SoundsActions(Arke.ARI.AriClient client)
        {
            _client = client ?? throw new ArgumentNullException(nameof(client));
        }

        public List<Sound> List(string? lang = null, string? format = null)
        {
            return ListAsync(lang: lang, format: format).GetAwaiter().GetResult();
        }

        public async Task<List<Sound>> ListAsync(string? lang = null, string? format = null)
        {
            var url = "sounds";
            if (!string.IsNullOrEmpty(lang)) url += (url.Contains("?") ? "&" : "?") + "lang=" + System.Uri.EscapeDataString(lang);
            if (!string.IsNullOrEmpty(format)) url += (url.Contains("?") ? "&" : "?") + "format=" + System.Uri.EscapeDataString(format);
            return await _client.GetAsync<List<Sound>>(url);
        }

        public Sound Get(string soundId)
        {
            return GetAsync(soundId: soundId).GetAwaiter().GetResult();
        }

        public async Task<Sound> GetAsync(string soundId)
        {
            var url = "sounds/{soundId}";
            return await _client.GetAsync<Sound>(url);
        }

    }
}
